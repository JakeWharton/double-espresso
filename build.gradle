allprojects {
  buildscript {
    repositories {
      jcenter()
    }
  }

  repositories {
    jcenter()
  }

  group = GROUP
  version = VERSION_NAME
}

ext {
  androidPlugin = 'com.android.tools.build:gradle:0.12.2'
  compileSdkVersion = 20
  buildToolsVersion = '20.0.0'
}
