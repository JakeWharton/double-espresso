allprojects {
  buildscript {
    repositories {
      jcenter()
    }
  }

  repositories {
    jcenter()
  }

  group = GROUP
  version = VERSION_NAME
}

ext {
  androidPlugin = 'com.android.tools.build:gradle:0.13.3'
  compileSdkVersion = 21
  buildToolsVersion = '21.0.2'
}
